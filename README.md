# UpdateFromPrivateGit
The script checks if there have been any modifications in the private repository and, if so, updates the local directory.

Required dependencies:
  - Python3
  - python3-pip
  - git

From pip:
  - GitPython
